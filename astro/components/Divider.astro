---
import type { SupportsColorway } from "../types";
import type { HTMLAttributes } from "astro/types";

interface Props extends SupportsColorway, Omit<HTMLAttributes<"hr">, "color"> {}

const { color, ...props } = Astro.props;
---

<hr {...props} class:list={[`lcars-${color}-border`]} />
